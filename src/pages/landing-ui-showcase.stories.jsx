import React from "react";
import Showcase from "./components/landing-ui-showcase.component";

export default {
  title: "Landing Page/Landing UI Showcase",
  component: Showcase,
};

const Template = (args) => <Showcase />;

export const LandingShowcase = Template.bind({});
